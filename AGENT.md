# AGENT.md: 厳格実装ガイドライン
このドキュメントは、本プロジェクトにおけるAIエージェントの挙動を縛るための絶対的な法典である。エージェントはコード生成時、以下のルールを1行たりとも違わずに遵守すること。

## 1. 設計の鉄則 (Core Design Principles)
### 単一責任の原則 (SRP)
- 1ファイル1責任: 1つのファイル、コンポーネント、関数は、たった一つの目的のために存在せよ。

- 100行の壁: UIコンポーネントが100行（コメント含む）を超えそうな場合、ロジックをCustom Hookへ、サブコンポーネントを別ファイルへ即座に切り出すこと。

### コロケーション (Colocation)
- 関連性は距離で示せ: 特定のコンポーネントのみで使用される Types, Styles, Components, Hooks は、グローバルなフォルダに置かず、そのコンポーネントと同じディレクトリ内に配置せよ。

- features/ ディレクトリ配下で機能ごとに完結させる構造を徹底すること。

### DRYの追求と過剰抽象化の禁止
- 2度あることは共通化: 全く同じロジックが2回出現した時点で utils または hooks へ抽出せよ。

- ただし、将来の変更可能性が異なる場合は、無理に共通化せず可読性を優先すること。

## 2. 記述・コメント規約 (Documentation Standard)
### 日本語による「1行レベル」の解説
- 全行解説: 複雑な条件分岐、状態更新、API通信、AIへのプロンプト構築ロジックには、原則として全ての行に日本語コメントを付与せよ。

- 「なぜ」の記述: コードを見ればわかる「何をしているか」だけでなく、その実装を選択した「理由（なぜ）」をコメントに含めること。

## JSDocの完全義務化
### 全ての関数およびコンポーネントの先頭に、以下の形式でJSDocを記述せよ。

```TypeScript
/**
 * クレーマーの攻撃性スコアに基づき、マスコットの表情ステートを決定する
 * @param score - 0.0から1.0の範囲の攻撃性数値
 * @returns 表情を示す識別子（'crying', 'shaking', 'default'）
 */
 ```

## 3. 実装上の厳格ルール (Strict Coding Rules)
### 型安全の徹底
- any の使用は重罪とする。全てのデータ構造に interface または type を定義せよ。

- APIレスポンスや外部入力など、不確実なデータには型ガードを実装し、実行時エラーを未然に防ぐこと。

### 状態管理の局所化
- 可能な限り useState を用いて状態を局所化せよ。複数のコンポーネントにまたがるマスコットの「感情値」などの共有データのみ、Context 等の適切な手段を用いること。

- ステート更新は副作用を最小限に抑え、冪等性を保つように実装せよ。

### エラーハンドリング
- 非同期処理（API通信等）には必ず try-catch を実装し、ユーザーに対して適切なエラーフィードバック（トースト通知やマスコットの困り顔など）を返せ。

- 予期せぬ入力に対しても、システム全体がクラッシュしないようガード節を多用せよ。

## 4. プロジェクト固有のロジック制約
- 攻撃性スコア (Aggression Score): 0.0（平常）から1.0（最大攻撃）の浮動小数点数で管理せよ。

- 非可逆的変換の禁止: クレーマーの言葉を「毒抜き」する際、本来の「要求（納期、返金、修正等）」を損なわないよう、変換ロジックのプロンプトを厳密に調整せよ。

- 視覚的フィードバック: UIの変更（赤転、振動、マスコットの涙）は、必ずこのスコアと連動させ、ロジックと演出を分離して管理せよ。